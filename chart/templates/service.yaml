apiVersion: v1
kind: Service
metadata:
  name: samsung-remote
spec:
  type: ClusterIP
  selector:
    app: samsung-remote
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
